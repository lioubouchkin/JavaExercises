package dictionnaryBinaryTree_exe;

import dictionaryBinaryTree.MySortedTree;

public class TreeBalance {
	
	// inorder tree traversal to count tree leaves
	public int countLeaves (MySortedTree.) {
		int leaves = 0; 
		if () {
			
		}
		return leaves;
	}
}
